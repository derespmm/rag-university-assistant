# =============================================================
#  RAG University Assistant — Backend Dependencies
#  Install via: pip install -r requirements.txt
# =============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- RAG / LangChain ---
langchain==0.3.13
langchain-openai==0.2.14
langchain-chroma==0.1.4
langchain-community==0.3.13

# --- Vector store ---
chromadb==0.6.3

# --- PDF parsing ---
pypdf==5.1.0
pdfplumber==0.11.4          # better table/layout extraction than pypdf alone

# --- OpenAI ---
openai==1.58.1

# --- File uploads & validation ---
python-multipart==0.0.20    # required for FastAPI file uploads
pydantic==2.10.4
pydantic-settings==2.7.0    # loads .env into Settings model

# --- Utilities ---
python-dotenv==1.0.1
tqdm==4.67.1                # progress bars for ingestion script
tiktoken==0.8.0             # token counting for chunking strategy

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1               # async test client for FastAPI